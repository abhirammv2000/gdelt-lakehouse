# Airflow providers (versions resolved via the 2.9.3 constraints file)
apache-airflow-providers-apache-spark
apache-airflow-providers-openlineage

# gdelt_pipeline runtime dependencies — the package is mounted, not installed,
# so its deps must be present in the image. Mirrors [project.dependencies].
httpx
tenacity
pydantic
pydantic-settings
fsspec
s3fs
typer
structlog
python-dotenv
